{
  "name": "catallaxy",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "npx tsx generate-compose.ts > docker-compose.yml",
    "start": "npx tsx generate-compose.ts > docker-compose.yml && docker compose build && docker compose up"
  },
  "dependencies": {
    "viem": "^2.47.5"
  }
}
