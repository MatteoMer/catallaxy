{
  "id": "task-002",
  "description": "Create a Python module 'fizzbuzz.py' at the repo root exporting a 'fizzbuzz(n: int) -> str' function. Returns 'Fizz' if n is divisible by 3, 'Buzz' if divisible by 5, 'FizzBuzz' if divisible by both, otherwise the number as a string. Tests in tests/test_fizzbuzz.py must pass.",
  "repo": "repos/playground",
  "base_branch": "main",
  "review_fee": 2000,
  "deterministic_checks": [
    {
      "type": "command",
      "cmd": "python3 -m unittest tests.test_fizzbuzz",
      "must_pass": true
    }
  ],
  "status": "open",
  "posted_by": "fixture",
  "posted_at": "1970-01-01T00:00:00.000Z",
  "deadline_at": "1970-01-01T00:05:00.000Z"
}
